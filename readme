Install git
Install git hub command line
gh auth login
gh clone
git submodule init
git submodlue update

install vscode
- install C/C++ for Visual Studio Code
- install Build Tools for Visual Studio
install meson

meson debug
meson compile -C debug

